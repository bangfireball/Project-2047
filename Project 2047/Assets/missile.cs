using UnityEngine;
using System.Collections;

public class missile : MonoBehaviour {

    private Transform targetPos; 

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        Vector2.MoveTowards(this.transform, this.transform, )
	}
}
